<% content_for(:title, t('.title')) %>
<div class="container">
  <h1><%= t '.title' %></h1>
  <!-- 掲示板一覧 -->
  <div class="row">
    <div class="col-12">
      <table class="table">
        <thead>
          <tr>
            <th scope="col">id</th>
            <th scope="col">タイトル</th>
            <th scope="col">作成者</th>
            <th scope="col">作成日時</th>
            <th scope="col"></th>
          </tr>
        </thead>
        <%= render @boards %>
      </table>
      <!-- ページネーション -->
      <%= paginate @boards %>
    </div>
  </div>
</div>
