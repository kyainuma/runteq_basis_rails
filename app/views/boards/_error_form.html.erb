<% if board.errors.any? %>
  <div class="alert alert-danger">
    <ul>
      <% board.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
