<%= link_to bookmark_path(current_user.bookmarks.find_by(board_id: board.id)), remote: true, id: "js-bookmark-button-for-board-#{board.id}", class:"float-right", method: :delete do %>
  <%= icon 'fas', 'star' %>
<% end %>
